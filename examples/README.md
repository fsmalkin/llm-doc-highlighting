# Examples

This folder contains small, static JSON examples to make the core artifacts concrete without shipping PDFs.

- `examples/geometry_index_minimal.json` - a tiny Geometry Index shape (page -> lines/words)
- `examples/resolve_output_example.json` - an example output from `scripts/resolve_highlight.py`

These are documentation artifacts (not meant to be executed as-is).

