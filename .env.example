# Secrets only. Put non-secret local config in .env.local (see .env.local.example).
# Do not commit real secrets (use `.env` / `.env.local`, which are git-ignored).
#
# Phase 2: LLM span citations (primary)
# OPENAI_API_KEY=sk-...
#
# Vision rails credentials (optional)
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
